export const initialState = {
  mainPosts: [],
}

const userReducer = ((state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
});

export default userReducer;